export const get = url => {
  return Promise.resolve('hi');
};
export const post = (url, body) => {
  return Promise.resolve('hi');
};
export const put = (url, body) => {
  return Promise.resolve('hi');
};
